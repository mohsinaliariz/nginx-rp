const beviConfig = (origin) => {
    return {
        id: 'fk-bevi-layout',
        background: `${origin}/bevi/background.png`,
        logo: `${origin}/bevi/logo.png`
    }
}

export default beviConfig
