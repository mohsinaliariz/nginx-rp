const defaultConfig = (origin) => {
    return {
        id: 'fk-default-layout',
        background: `${origin}/default/background.png`,
        logo: `${origin}/default/logo.png`
    }
}

export default defaultConfig
