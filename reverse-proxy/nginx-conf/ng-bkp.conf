worker_processes auto;

error_log  /var/log/nginx/error.log;
pid        /var/run/nginx-fe.pid;
worker_rlimit_nofile 65532;

events {
    worker_connections 8192;
}

http {


    # Determine the upstream of three services
    # upstream reverse-proxy {
    #     # this will point to the Docker Container DNS which is the container name and the port it exposes
    #     server reverse-proxy:80;
    # }

#    upstream cc-apps-rewards {
 #       server rewards:5002;
  #  }

    upstream cc-apps-experiences {
    server experiences:5004;
    }


    # upstream service-three {
     #    server dev.ccapps.fankave.com/databricks/testimonials;
    # }


    # Write the reverse proxy
    server {
        # Determine where to output the log
        access_log /var/log/nginx/access.log;
        # expose port 80
        listen 80;

        # if the root route get access it will return the default nginx html page
        location / {
            root   /usr/share/nginx/html;
            index  index.html index.htm;
        }

        # Here we do the reverse proxy with the keyword "proxy_pass" and pass the upstream as the first argument
        # do not forget to set the proxy header, here I will use the common header instructed by the documentation
        location /autodesk {
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            rewrite ^/experiences/autodesk(.*)$ /$1 break;
            # do not forget to include the scheme which is http
    		proxy_pass http://cc-apps-experiences/autodesk;
    	}

        location /test {
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $http_host;
            proxy_set_header X-Forwarded-Proto $scheme;
            rewrite ^/experiences/autodesk(.*)$ /$1 break;
            # do not forget to include the scheme which is http
    		proxy_pass http://cc-apps-experiences/autodesk;
    	}

        location /external-autodesk {
            #proxy_set_header X-Real-IP $remote_addr;
            #proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            #proxy_set_header Host $http_host;
            #proxy_set_header X-Forwarded-Proto $scheme;
            rewrite ^https://dev.ccapps.fankave.com/experiences/autodesk(.*)$ /$1 break;
            # do not forget to include the scheme which is http
	    proxy_pass https://dev.ccapps.fankave.com/experiences/autodesk;
    	}

	location /external-testimonial {
        #    proxy_set_header X-Real-IP $remote_addr;
            #proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
           # proxy_set_header Host $http_host;
            #proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Host "dev.ccapps.fankave.com";
	    rewrite ^https://dev.ccapps.fankave.com/databricks/testimonials(.*)$ /$1 break;
            # do not forget to include the scheme which is http
            proxy_pass https://dev.ccapps.fankave.com/databricks/testimonials;
        }

	location /databricks {
	    proxy_pass https://dev.ccapps.fankave.com/databricks;
	    #rewrite ^https://dev.ccapps.fankave.com/databricks/testimonials(.*)$ /$1 break;
	    proxy_set_header Host "dev.ccapps.fankave.com";
	}

        location /static/css {
            proxy_pass https://dev.ccapps.fankave.com/databricks/testimonials/static/css;
            add_header  Content-Type    text/css;

        }
        location /static/js {
            proxy_pass https://dev.ccapps.fankave.com/databricks/testimonials/statics/js;
            add_header  Content-Type    application/x-javascript;
        } 

        # do the same with service two and service three
        

        # location /three {
        #     proxy_set_header X-Real-IP $remote_addr;
        #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        #     proxy_set_header Host $http_host;
        #     proxy_set_header X-Forwarded-Proto $scheme;
    	# 	proxy_pass http://service-three;
    	# }
    }
}    
